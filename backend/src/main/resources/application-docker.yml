spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/receivables
    username: receivables
    password: receivables
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
